# invalid YAML b/c paths is missing a :
base_url: "https://example.com"
paths
  /person:
    post:
      operationId:
      steps:
        - name: createPerson
          method: POST
          headers:
            Content-Type: application/json
          data:
            name: "John Doe"
            age: 42
    get:
      operationId: getPerson
      steps:
        - name: createPerson
          method: POST
          headers:
            Content-Type: application/json
          data:
            name: "John Smith"
            age: 30
        - name: getPerson
          method: "GET"
          url_parameters:
            id: "{{ createPerson.response.id }}"
          headers:
            Content-Type: application/json
